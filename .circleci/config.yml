# DEPRECATED: CI has migrated to GitHub Actions.
# See .github/workflows/ci.yml for the current CI configuration.
#
# This file is retained temporarily to prevent CircleCI from
# auto-detecting a missing config as an error. It can be safely
# removed once the GitHub Actions workflow is confirmed stable.
#
# Migration summary:
#   - 12 test jobs → GitHub Actions matrix (13 jobs + lint + validation)
#   - Inline steps → reusable scripts in scripts/ci/
#   - Allure (all jobs) → Allure (py312 only) + JUnit XML (all others)
#   - Coverage upload (all jobs) → Coverage upload (3 representative jobs)
#
version: 2.1
workflows:
  noop:
    jobs: []
